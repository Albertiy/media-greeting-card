const GlobalSettings = {
    siteTitle: str => '' + str + ' - 贺卡',
    modifyToken: 'modify_token',
    accessToken: 'access_token',
};

// console.log(GlobalSettings.siteTitle('Hello'));

export default GlobalSettings;